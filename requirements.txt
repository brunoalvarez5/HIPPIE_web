# Streamlit — pinned: st.bokeh_chart support is version-coupled to bokeh 2.4.3
streamlit==1.43.0

# Core stack
numpy>=1.26,<2
scipy>=1.11
pandas>=1.5
scikit-learn>=1.2

# UMAP stack — numba/llvmlite have no Python 3.13+ wheels; app must run on ≤3.12
umap-learn>=0.5.4
pynndescent>=0.5
numba>=0.58
llvmlite>=0.41

# Inference — no PyTorch needed, model runs entirely through ONNX Runtime
onnxruntime>=1.16.3

# Visualization
bokeh==2.4.3  # exact pin required: st.bokeh_chart rejects any other bokeh version
altair>=5.0
matplotlib>=3.7

# IO / utils
h5py>=3.10
hdmf>=4.1.0
pynwb>=3.1.2
joblib>=1.3
requests>=2.32
hdbscan>=0.8.33
gdown
